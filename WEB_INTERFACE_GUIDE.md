# Web界面使用指南

## 新增功能

### 1. 语音输入识别
- **功能**：用户可以通过语音输入指令，无需手动打字
- **使用方法**：
  - 点击输入框右侧的"🎤 语音"按钮
  - 按钮变为绿色"🎤 听取中"状态，开始说话
  - 系统会自动识别语音并转换为文字
  - 识别完成后自动发送指令
- **支持语言**：中文（zh-CN）
- **技术**：使用浏览器原生 Web Speech API

### 2. 语音反馈功能
- **功能**：机器人回复时可以同步进行语音朗读
- **使用方法**：
  - 点击界面右上角的"语音反馈"开关
  - 开关变为激活状态（滑块向右）
  - 之后所有机器人的文本回复都会自动朗读
  - 再次点击可关闭语音反馈
- **特点**：
  - 可选开启/关闭
  - 保留原有文本显示
  - 使用中文语音合成
- **技术**：使用浏览器原生 Web Speech Synthesis API

### 3. 图片输出支持
- **功能**：显示D455摄像头采集的实时图像
- **界面布局**：
  - 右侧新增摄像头面板
  - 显示实时摄像头视图
  - 提供"刷新图像"按钮
- **自动刷新**：
  - 页面加载时自动获取图像
  - 执行视觉相关指令后自动刷新（如"看"、"识别"、"拿"等）
- **交互**：
  - 点击图片可在新窗口查看大图
  - 手动点击"刷新图像"按钮更新视图

## 启动方法

```bash
# 1. 确保Bridge Server正在运行
cd /home/cothink/Baxter-claw
python start_server.py

# 2. 启动Web界面
python openclaw_plugin/web_interface.py

# 3. 在浏览器打开
http://localhost:5000
```

## 界面布局

```
┌─────────────────────────────────────────────────────────────┐
│  🤖 Baxter-Claw                    [语音反馈 开关]          │
│  通过自然语言控制机器人                                      │
├─────────────────────────────┬───────────────────────────────┤
│                             │  📷 摄像头视图                 │
│  聊天对话区域                │                               │
│  - 显示用户输入              │  [摄像头图像显示区域]          │
│  - 显示机器人回复            │                               │
│  - 支持图片显示              │                               │
│                             │  [刷新图像]                    │
├─────────────────────────────┴───────────────────────────────┤
│  [输入框]  [🎤 语音]  [发送]                                │
│  示例: [拿杯子] [Pick box] [放到左边] ...                   │
└─────────────────────────────────────────────────────────────┘
```

## 浏览器兼容性

### 语音识别支持
- ✅ Chrome/Edge (推荐)
- ✅ Safari (部分支持)
- ❌ Firefox (不支持 Web Speech API)

### 语音合成支持
- ✅ Chrome/Edge
- ✅ Safari
- ✅ Firefox

### 建议
使用 **Chrome** 或 **Edge** 浏览器以获得最佳体验。

## 示例使用场景

### 场景1：语音控制抓取
1. 点击"🎤 语音"按钮
2. 说："帮我拿一下红色的杯子"
3. 系统识别语音并自动发送
4. 机器人执行抓取任务
5. 摄像头自动刷新显示当前画面

### 场景2：带语音反馈的物体识别
1. 开启右上角"语音反馈"开关
2. 输入或语音："看看桌上有什么"
3. 机器人识别物体并文字回复
4. 同时语音朗读识别结果
5. 摄像头显示识别时的图像

### 场景3：实时监控
1. 点击"刷新图像"按钮
2. 查看当前摄像头画面
3. 根据画面给出指令
4. 点击图片可放大查看细节

## 技术实现

### 后端API
- `POST /chat` - 处理聊天消息
- `GET /camera/image` - 获取摄像头图像（base64编码）
- `GET /status` - 检查服务状态

### 前端技术
- **语音识别**：Web Speech Recognition API
- **语音合成**：Web Speech Synthesis API
- **图像显示**：Base64编码的JPEG图像
- **响应式布局**：Flexbox布局，支持不同屏幕尺寸

## 注意事项

1. **语音识别**：
   - 需要浏览器麦克风权限
   - 首次使用会弹出权限请求
   - 需要在安静环境中使用以获得最佳识别效果

2. **摄像头图像**：
   - 需要Bridge Server正常运行
   - 需要D455摄像头正确连接
   - 图像获取可能需要1-2秒

3. **语音反馈**：
   - 可能会与系统其他音频冲突
   - 建议在需要时才开启
   - 朗读速度和音量可在代码中调整

## 故障排除

### 语音识别不工作
- 检查浏览器是否支持（使用Chrome/Edge）
- 检查麦克风权限是否授予
- 尝试刷新页面重新授权

### 摄像头图像无法显示
- 检查Bridge Server是否运行（http://localhost:8420）
- 检查D455摄像头是否连接
- 查看浏览器控制台错误信息

### 语音反馈无声音
- 检查系统音量设置
- 检查浏览器是否静音
- 尝试关闭再重新开启语音反馈

## 未来改进方向

- [ ] 支持多语言切换（中文/英文）
- [ ] 添加语音识别语言自动检测
- [ ] 支持连续对话模式
- [ ] 添加摄像头视频流实时显示
- [ ] 支持多摄像头切换
- [ ] 添加语音指令快捷方式
- [ ] 优化移动端体验
